export interface Analytics {
    cases: number | null,
    negative: number | null,
    positive: number | null,
    totalTests: number | null,
    hospitalizedCur: number | null,
    hospitalizedCul: number | null, 
    icuCur: number | null,
    icuCul: number | null,
    ventCur: number | null,
    ventCul: number | null,
    recovered: number | null,
    deaths: number | null
}
